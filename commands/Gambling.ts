// import { CommandInteraction } from "discord.js";
// import { Discord, Slash, SlashOption } from "discordx";

// export abstract class Casino {
//     @Slash("balance", { description: "Check a user's balance" })
//     private async balance(
//         @SlashOption("user", { description: "The user whose balance to check" }) user: string,
//         ctx: CommandInteraction): Promise<void> {

//     }
// }